assets/_orig/
.git
# The home page is served by the CMS publish (vercel.json rewrite / -> trainr-home).
# Vercel serves static files BEFORE rewrites, so the ROOT index.html must not ship in
# this deploy. Anchored with the leading slash so blog/index.html still deploys.
/index.html
